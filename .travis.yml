sudo: true
language: python
dist: xenial
python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7"

# command to install dependencies
install:
  - python setup.py install

# command to run tests
script:
  - python setup.py test